You are working in a client called heheboi client. You must write human code, without unecessary comments.

## Build

```bash
cd "D:\_edward\projects\eduHacksss" && cmake --build build-clang 2>&1 | tail -3
```
